import Hero from './Hero';

const HomePage = () => {
  return (
    <section
      id="home"
      className="flex justify-center items-center flex-col w-full h-screen text-center"
    >
      <Hero />
    </section>
  );
};

export default HomePage;
